-- pop-punk.vim
-- https://github.com/bignimbus/pop-punk.vim

return {
  'bignimbus/pop-punk.vim',
  config = function()
    -- -- Set the colorscheme
    -- vim.cmd("colorscheme pop-punk")
    --
    -- -- Set terminal ANSI colors using pop_punk's function.
    -- -- We call the vimscript function pop_punk#AnsiColors() via vim.fn.
    -- vim.g.terminal_ansi_colors = vim.fn['pop_punk#AnsiColors']()
    --
    -- -- Configure the airline theme
    -- vim.g.airline_theme = 'pop_punk'
    -- vim.g.airline_section_c = '🎸 %F'
    --
    -- -- Configure the lightline theme
    -- vim.g.lightline = { colorscheme = 'pop_punk' }
  end,
}
